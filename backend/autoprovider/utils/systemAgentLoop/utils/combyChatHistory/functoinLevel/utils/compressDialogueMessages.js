const compressDialogueMessages = async (
  dialogueHistoryMessages,
  infoObject
) => {
  //调用小模型，对这个消息进行简练提取处理
  const results = [];

  //将AI提炼好的内容转成messages格式
  const messages = "";

  //将提炼好的消息return回去
  return results;
};
